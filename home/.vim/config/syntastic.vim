" Setup Syntastic plugin options ---------------------------------------------------------------------------------------

" Only lint PHP files within the editor.
let g:syntastic_php_checkers = ['php']

